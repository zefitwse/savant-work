# Model Files

把模型文件放在这里，或者在配置文件中使用绝对路径。

常见文件：

- `*.onnx`
- `*.engine`
- `labels.txt`
- calibration cache

课程演示阶段可以先使用 DeepStream 容器内自带的 sample 模型，把 `configs/pgie_config.txt` 和 `configs/sgie_config.txt` 中的路径改成容器内路径即可。

